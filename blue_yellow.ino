void runYellowBlue () { 
  setAll(0,0,255);

  for (int a = 10; a < 30; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 50; a < 70; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 90; a < 110; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 130; a < 150; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 170; a < 190; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 210; a < 230; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 250; a < 270; a++) {
    setPixel(a, 255 , 255, 0);
  }
  for (int a = 290; a < 300; a++) {
    setPixel(a, 255 , 255, 0);
  }
  showStrip();
}
